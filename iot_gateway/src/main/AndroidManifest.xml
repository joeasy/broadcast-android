<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nbplus.iotgateway" >

    <!-- Declare this required feature if you want to make the app available to BLE-capable
    devices only.  If you want to make your app available to devices that don't support BLE,
    you should omit this in the manifest.  Instead, determine BLE capability by using
    PackageManager.hasSystemFeature(FEATURE_BLUETOOTH_LE) -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name" >
        <activity
            android:name=".GatewaySettingsActivity"
            android:label="@string/title_activity_gateway_settings" >
        </activity>
        <service android:exported="false" android:name=".service.IoTService">
            <intent-filter>
                <action android:name="com.nbplus.iotgateway.action.START_IOT_SERVICE" />
                <action android:name="com.nbplus.iotgateway.action.IOT_DEVICE_LIST" />
                <action android:name="com.nbplus.iotgateway.action.SEND_IOT_COMMAND" />
                <action android:name="com.nbplus.iotgateway.action.IOT_DEVICE_LIST" />
                <action android:name="com.nbplus.iotgateway.action.IOT_GATEWAY_DATA" />
                <action android:name="com.nbplus.iotgateway.action.UPDATE_IOT_DEVICE_DATA" />
            </intent-filter>
        </service>
    </application>

</manifest>

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:orientation="vertical"
              style="@android:style/Widget.Button"
              android:clickable="true"
              android:background="@drawable/ic_apps_grid_selector"
              android:padding="5dp" >

    <ImageView
        android:id="@+id/grid_item_image"
        android:layout_width="@dimen/installed_apps_list_icon_size"
        android:layout_height="@dimen/installed_apps_list_icon_size"
        android:layout_margin="5dp"
        android:scaleType="fitXY"
        android:layout_gravity="center"
        >
    </ImageView>

    <TextView
        android:id="@+id/grid_item_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textAlignment="center"
        android:ellipsize="end"
        android:lines="2"
        android:gravity="center"
        android:layout_gravity="center"
        android:textSize="@dimen/installed_apps_list_font_size" >
    </TextView>

</LinearLayout>
